(defdomain disjunction (

  (:- (axiom1 ?a ?b ?c)
    (
      (p ?a ?b)
      (or
        (q ?a)
        (r ?b)
        (s ?c)
      )
    )
  )

  (:operator (!op ?z ?x ?y)
    ; Preconditions
    ()
    ; Del effects
    ()
    ; Add effects
    ()
  )

  (:method (unify ?z)
    ; Preconditions
    (
      (pred ?z)
      (axiom1 ?z ?x ?y) ; TODO x and y unification inside axiom
    )
    ; Subtasks
    (
      (op ?z ?x ?y)
    )
  )
))